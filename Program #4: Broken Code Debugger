//Constantine Nikolov

const readlineSync = require('readline-sync');
let start = readlineSync.question("Enter start number: ");
let end = readlineSync.question("Enter end number: ");
let count = 0;

for (let i = start; i <= end; i++) {
    //Changed if argument to "i % 2" instead of "i / 2"
    if (i % 2 == 0) {
    //Changed count variable to "count += 1" instead of "count =+ 1"
        count += 1;
    }
}

console.log("Even numbers between " + start + " and " + end + ": " +
count);
